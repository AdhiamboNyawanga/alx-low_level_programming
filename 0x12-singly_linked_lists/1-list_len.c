#include "lists.h"

/**
 * list_len - prints all the elements of a list_t list.
 * @x: singly linked list.
 * 
 */
size_t list_len(const list_t *x)
{
size_t syp;

syp = 0;
while (x != NULL)
{
syp++;
h = h->next;
}
return (syp);
}
